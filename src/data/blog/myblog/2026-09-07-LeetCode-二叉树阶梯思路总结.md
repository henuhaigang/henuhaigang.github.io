---
layout:     post
title:      LeetCode 二叉树阶梯思路总结
pubDatetime: 2026-09-03T09:47:56.000Z
modDatetime: 2026-09-03T09:47:56.000Z
author:     henuhaigang
header-img: img/post-bg-algorithm.jpg
catalog: true
tags:
    - 算法
    - LeetCode
    - 二叉树
description: 二叉树核心遍历模式、递归/迭代技巧与阶梯刷题路线总结
---

# LeetCode 二叉树阶梯思路总结

二叉树是面试高频考点，**递归思维**与**遍历变体**是核心。本文梳理 4 大遍历体系、5 类核心题型、必刷题单与通用模板。

---

## 一、二叉树四大遍历体系（必须烂熟于心）

### 1. 深度优先遍历（DFS）—— 递归版
```python
# 前序：根 → 左 → 右
def preorder(root):
    if not root: return
    process(root.val)
    preorder(root.left)
    preorder(root.right)

# 中序：左 → 根 → 右（BST 升序）
def inorder(root):
    if not root: return
    inorder(root.left)
    process(root.val)
    inorder(root.right)

# 后序：左 → 右 → 根（底-up 信息传递）
def postorder(root):
    if not root: return
    postorder(root.left)
    postorder(root.right)
    process(root.val)
```

### 2. DFS —— 迭代版（栈模拟）
```python
# 前序：栈先压右再压左
def preorder_iter(root):
    if not root: return []
    stack, res = [root], []
    while stack:
        node = stack.pop()
        res.append(node.val)
        if node.right: stack.append(node.right)
        if node.left: stack.append(node.left)
    return res

# 中序：左尽头入栈
def inorder_iter(root):
    stack, res = [], []
    cur = root
    while cur or stack:
        while cur:
            stack.append(cur)
            cur = cur.left
        cur = stack.pop()
        res.append(cur.val)
        cur = cur.right
    return res

# 后序：前序根右左 → 反转
def postorder_iter(root):
    if not root: return []
    stack, res = [root], []
    while stack:
        node = stack.pop()
        res.append(node.val)
        if node.left: stack.append(node.left)
        if node.right: stack.append(node.right)
    return res[::-1]
```

### 3. 广度优先遍历（BFS）—— 层序遍历
```python
from collections import deque

def level_order(root):
    if not root: return []
    q, res = deque([root]), []
    while q:
        level = []
        for _ in range(len(q)):
            node = q.popleft()
            level.append(node.val)
            if node.left: q.append(node.left)
            if node.right: q.append(node.right)
        res.append(level)
    return res
```

### 4. 统一迭代模板（带访问标记——通用性最强）
```python
def traversal(root):
    if not root: return []
    stack, res = [(root, False)], []
    while stack:
        node, visited = stack.pop()
        if not node: continue
        if visited:
            res.append(node.val)  # 后序位置
        else:
            stack.append((node.right, False))
            stack.append((node.left, False))
            stack.append((node, True))  # 标记为已访问子节点
            # 前序位置：res.append(node.val) 放在这里
            # 中序位置：放在 left 和 right 之间
    return res
```

---

## 二、五大核心题型模型

| 题型 | 核心思路 | 遍历方式 | 典型题目 |
|------|----------|----------|----------|
| **构造/修改树** | 前序「根左右」构造，返回子树根节点 | 前序（自顶向下） | 105, 106, 654, 889 |
| **性质判定/计算** | 后序「左右根」汇总子树信息，自底向上 | 后序（自底向上） | 104, 110, 226, 124, 543, 236 |
| **路径/和问题** | 前序传路径/和，回溯撤销；或后序返回贡献 | 前序+回溯 / 后序 | 112, 113, 437, 129, 687 |
| **BST 专属** | 中序升序、区间约束、利用有序性 | 中序 / 前序+区间 | 98, 230, 700, 701, 450, 538 |
| **层序/视图/宽度** | 队列 BFS，逐层处理 | 层序 | 102, 107, 199, 515, 662 |

---

## 三、通用递归框架（套用即得）

### 1. 自顶向下传参数（前序位置处理）
```python
def dfs(node, param):
    if not node: return base_case
    # 前序：处理当前节点，更新参数
    new_param = update(param, node)
    if is_leaf(node): 
        update_global_answer(new_param)
    dfs(node.left, new_param)
    dfs(node.right, new_param)
    # 无需返回值，靠全局变量/引用传递结果
```

### 2. 自底向上返回值（后序位置处理）
```python
def dfs(node):
    if not node: return base_info  # 如 0, -inf, True, None
    left_info = dfs(node.left)
    right_info = dfs(node.right)
    # 后序：根据左右子树信息计算当前节点信息
    cur_info = merge(left_info, right_info, node)
    update_global_answer(cur_info)  # 可选
    return cur_info
```

### 3. BST 区间约束模板
```python
def isValidBST(root):
    def dfs(node, low, high):
        if not node: return True
        if not (low < node.val < high): return False
        return dfs(node.left, low, node.val) and dfs(node.right, node.val, high)
    return dfs(root, float('-inf'), float('inf'))
```

---

## 四、阶梯刷题路线图

### 🟢 入门级（遍历+基础性质，~15 题）
| 序号 | 题目 | 核心考点 |
|------|------|----------|
| 1 | 144. 二叉树的前序遍历 | 递归/迭代/统一模板 |
| 2 | 94. 二叉树的中序遍历 | BST 升序验证 |
| 3 | 145. 二叉树的后序遍历 | 后序模板 |
| 4 | 102. 二叉树的层序遍历 | BFS 队列逐层 |
| 5 | 104. 二叉树的最大深度 | 后序返回深度 / 前序传深度 |
| 6 | 111. 二叉树的最小深度 | 注意单子树情况 |
| 7 | 226. 翻转二叉树 | 前序交换左右 / 后序返回新根 |
| 8 | 100. 相同的树 | 同步遍历比较 |
| 9 | 101. 对称二叉树 | 双树同步/队列成对入队 |
| 10 | 572. 另一棵树的子树 | 序列化比较 / 双递归 |
| 11 | 110. 平衡二叉树 | 后序返回高度，-1 标记不平衡 |
| 12 | 257. 二叉树的所有路径 | 前序回溯路径字符串 |
| 13 | 404. 左叶子之和 | 判断 `node.left and not node.left.left and not node.left.right` |
| 14 | 617. 合并二叉树 | 前序创建新节点 / 复用原节点 |
| 15 | 700. 二叉搜索树中的搜索 | BST 性质单支查找 |

### 🟡 进阶级（路径和+构造+修改+LCA，~25 题）
| 序号 | 题目 | 核心考点 |
|------|------|----------|
| 16 | 112. 路径总和 | 前序传累加和 |
| 17 | 113. 路径总和 II | 前序回溯收集路径 |
| 18 | 437. 路径总和 III | 前缀和哈希表 / 双重递归 |
| 19 | 124. 二叉树中的最大路径和 | 后序返回「单边最大贡献」，全局维护「过根最大」 |
| 20 | 129. 求根节点到叶节点数字之和 | 前序传数字 `cur*10+val` |
| 21 | 687. 最长同值路径 | 后序返回「同值单边长度」 |
| 22 | 543. 二叉树的直径 | 后序返回深度，全局维护 `left+right` |
| 23 | 105. 从前序与中序构造二叉树 | 前序第一个是根，中序分左右，切片/索引映射 |
| 24 | 106. 从中序与后序构造二叉树 | 后序最后一个是根 |
| 25 | 889. 根据前序和后序构造二叉树 | 非唯一，约定左子树根在前序第二个 |
| 26 | 654. 最大二叉树 | 单调栈 O(n) / 递归 O(n²) |
| 27 | 236. 二叉树的最近公共祖先 | 后序：左有右有→根，左有→左，右有→右，无→None |
| 28 | 1650. 二叉树的最近公共祖先 III | 带父指针，哈希集合记录祖先 |
| 29 | 235. BST 的最近公共祖先 | 利用 BST 有序性：val 在分叉点即 LCA |
| 30 | 98. 验证二叉搜索树 | 中序前驱比较 / 区间约束递归 |
| 31 | 230. BST 第 K 小 | 中序计数 / 朝鲜遍历 |
| 32 | 538. 把二叉搜索树转换为累加树 | 逆中序（右根左）累加 |
| 33 | 701. BST 插入操作 | 找到叶子位置挂上 |
| 34 | 450. BST 删除节点 | 三种情况：无子/单子/双子（找后继/前驱替换） |
| 35 | 99. 恢复二叉搜索树 | 中序找两个逆序对，Morris O(1) 空间 |
| 36 | 108. 将有序数组转为 BST | 中序中点做根，递归构建 |
| 37 | 109. 有序链表转 BST | 快慢找中点 / 数组转列表 |
| 38 | 863. 二叉树中所有距离为 K 的结点 | 建图+父指针/BFS |
| 39 | 116. 填充每个节点的下一个右侧节点指针 | 完全二叉树利用 next 指针 O(1) 空间 |
| 40 | 117. 填充每个节点的下一个右侧节点指针 II | 普通树层序/虚拟头节点 |

### 🔴 高阶级（硬题/序列化/动态规划/单调栈，~15 题）
| 序号 | 题目 | 核心考点 |
|------|------|----------|
| 41 | 297. 二叉树的序列化与反序列化 | 前序+分隔符/# 标记空 / BFS 层序 |
| 42 | 337. 打家劫舍 III | 树形 DP：返回 `[不偷, 偷]` |
| 43 | 1245. 树的直径 | 两次 BFS / 后序 DP |
| 44 | 549. 二叉树中最长连续序列 | 后序返回以节点为端点的最长连续长度 |
| 45 | 687. 最长同值路径 | 后序 DP 变形 |
| 46 | 968. 监控二叉树 | 贪心/DP 三状态：0无覆盖 1有摄像头 2已覆盖 |
| 47 | 1373. 二叉搜索子树的最大键值和 | 后序返回 `[isBST, min, max, sum]` |
| 48 | 1379. 找出克隆二叉树中的相同节点 | 同步遍历原树和克隆树 |
| 49 | 1602. 找到处理最多请求的服务器 | 堆+树/线段树 |
| 50 | 2265. 统计值等于子树平均值的节点数 | 后序返回 `[sum, count]` |
| 51 | 2509. 查询树中环的长度 | 并查集/树上倍增 LCA |
| 52 | 2583. 二叉树中第 K 大的层和 | BFS 层和 + 堆/快选 |
| 53 | 2646. 最小化旅行的价格总和 | 树形 DP + 重根换根 |
| 54 | 2925. 在树上执行操作以后得到的最大分数 | 树形 DP 选/不选 |
| 55 | 面试题 04.06. 后继者 | BST 中序后继 / 父指针 |

---

## 五、高频易错点速查表

| 易错点 | 正确做法 |
|--------|----------|
| **最小深度单子树算错** | `if not left: return right+1` 等单子树特判，不能 `min(left, right)+1` |
| **路径回溯忘记撤销** | `path.append(val)` → 递归 → `path.pop()` |
| **前缀和哈希表忘记回溯** | `cnt[cur_sum] += 1` → 递归 → `cnt[cur_sum] -= 1` |
| **BST 验证用局部比较** | 必须用「区间约束」或「中序前驱」，不能只比父节点 |
| **LCA 返回值语义不清** | 明确：返回「找到的目标节点 / LCA / None」三态 |
| **构造树切片开销大** | 用索引 `(pre_l, pre_r, in_l, in_r)` + 哈希表存中序索引，O(n) 时间 |
| **层序遍历层分隔符丢失** | `for _ in range(len(q))` 固定当前层大小 |
| **Morris 遍历指针改错** | 先找前驱 `pre.right = cur`，访问后恢复 `pre.right = None` |
| **序列化空节点不标记** | 必须用 `#` 或 `null` 标记空，否则反序列化歧义 |
| **递归深度超限** | Python 改 `sys.setrecursionlimit` 或改迭代/BFS |

---

## 六、复杂度速记表

| 操作 | 平均时间 | 最坏时间 | 空间 | 备注 |
|------|----------|----------|------|------|
| 遍历（递归/迭代） | O(n) | O(n) | O(h) 栈 / O(n) 栈 | h 为高度 |
| 层序遍历 | O(n) | O(n) | O(w) 队列 | w 最大宽度 |
| BST 查找/插入/删除 | O(log n) | O(n) | O(h) | 平衡树 O(log n) |
| LCA（递归） | O(n) | O(n) | O(h) | - |
| 序列化/反序列化 | O(n) | O(n) | O(n) | - |
| 树形 DP | O(n) | O(n) | O(h) | 每节点常数次合并 |

---

## 七、面试实战技巧

1. **先确定遍历顺序**：
   - 构造树/传参数 → **前序**
   - 求性质/汇总信息/LCA/DP → **后序**
   - BST 有序/第 K 小 → **中序**
   - 层级/宽度/视图 → **层序**

2. **明确递归函数语义**：写注释 `return 以 node 为根的子树的 xxx`

3. **画图演示小树**：3-5 节点树手推递归调用栈、返回值流向

4. **双递归模式**：
   - 外层遍历每个节点作为起点
   - 内层从该节点出发 DFS
   - 如 437 路径总和 III、543 直径

5. **空间优化套路**：
   - 递归 → 迭代栈
   - 栈 → Morris 遍历（仅中序/前序）
   - BFS 队列 → 利用 `next` 指针 O(1) 空间（完全二叉树）

6. **树形 DP 状态设计**：
   - 打家劫舍：`dp[node][0/1]`
   - 监控二叉树：三状态贪心
   - 最大 BST 子树：返回 `[isBST, min, max, sum, size]`

---

## 八、一周速成计划（建议）

| 天数 | 任务 |
|------|------|
| Day 1 | 背诵 4 种遍历模板（递归+迭代+统一）+ 做 144, 94, 145, 102, 104, 111, 226 |
| Day 2 | 后序自底向上专项：110, 543, 124, 687, 236, 235 + 总结「后序返回值」模式 |
| Day 3 | 前序回溯/路径专项：112, 113, 437, 129, 257, 404 + 掌握前缀和优化 |
| Day 4 | BST 专项：98, 230, 700, 701, 450, 538, 99, 108 + 区间约束模板 |
| Day 5 | 构造/修改树：105, 106, 889, 654, 617, 100, 101, 572 + 索引映射优化 |
| Day 6 | 层序/视图/宽度/进阶：107, 199, 515, 662, 116, 117, 863, 297 |
| Day 7 | 硬题/树形 DP/模拟：337, 968, 1373, 2265 + 随机抽 3 道 30 分钟闭卷 |

---

> **一句话总结**：二叉树的本质是**递归分治**，前序「根左右」构造/传参，后序「左右根」汇总/判定，中序「左根右」BST 有序，层序「逐层」视图/宽度。把 4 种遍历、2 种递归框架、5 大题型模型刻进肌肉记忆，见题先问「要不要回溯」「要不要自底向上」「是不是 BST」，二叉树题就稳了。

---

*更新于 2026-09-07 | 持续补充中...*