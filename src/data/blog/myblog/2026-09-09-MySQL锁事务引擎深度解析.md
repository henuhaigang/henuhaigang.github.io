---
layout:     post
title:      MySQL 锁、事务、引擎深度解析：从 InnoDB 架构到线上死锁排查实战
pubDatetime: 2026-09-09T14:00:00.000Z
modDatetime: 2026-09-09T14:00:00.000Z
author:     henuhaigang
header-img: img/post-bg-mysql.jpg
catalog: true
tags:
    - MySQL
    - InnoDB
    - 锁
    - 事务
    - MVCC
    - 死锁
    - 性能优化
description: 12年 DBA 视角深度剖析 MySQL InnoDB 锁体系、MVCC 实现、事务隔离级别、死锁原理与排查、引擎选型、线上典型事故案例，附核心源码逻辑与最佳实践
---

# MySQL 锁、事务、引擎深度解析

> **核心认知**：MySQL 的「锁」不是单一概念，而是 **全局锁 → 表锁 → 行锁 → 意向锁 → 间隙锁 → Next-Key Lock** 的层级防御体系；MVCC 不是魔法，是 **Undo Log + ReadView + 隐式主键** 的精密协作；死锁不是bug，是高并发下的必然概率事件，关键是「如何快速发现、如何优雅恢复」。

---

## 一、InnoDB 架构与锁体系全景图

```
┌─────────────────────────────────────────────────────────────────┐
                        MySQL Server Layer
├─────────────────────────────────────────────────────────────────┤
  Connection Pool  →  Parser  →  Optimizer  →  Executor
                                                          │
                                                          ▼
┌─────────────────────────────────────────────────────────────────┐
                        InnoDB Storage Engine
├─────────────────────────────────────────────────────────────────┤
  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │ Buffer Pool  │  │  Lock Sys    │  │  Log Sys     │
  │ (LRU/Flush)  │◄─│ (行锁/表锁/  │◄─│ (Redo/Undo)  │
  │              │  │  意向/间隙)  │  │              │
  └──────────────┘  └──────────────┘  └──────────────┘
        ▲                 ▲                 ▲
        │                 │                 │
  ┌─────┴─────┐   ┌───────┴───────┐  ┌──────┴──────┐
  │ Data Page │   │ Lock Struct   │  │ Log Buffer  │
  │ (B+Tree)  │   │ (Hash表+链表) │  │ (顺序写)    │
  └───────────┘   └───────────────┘  └─────────────┘
```

### 1.1 锁的层级与兼容性矩阵

| 锁类型 | 粒度 | 作用范围 | 兼容性 | 典型场景 |
|--------|------|----------|--------|----------|
| **全局锁** | 实例 | 全库 | 互斥 | `FLUSH TABLES WITH READ LOCK` 备份 |
| **表锁** | 表 | 整张表 | 互斥 | DDL、全表扫描无索引 |
| **意向锁** | 表 | 标识行锁意图 | IX/IS 兼容，X/S 互斥 | 行锁前置检查 |
| **记录锁** | 行 | 单行记录 | X 互斥，S 兼容 | `WHERE id = 1` 命中唯一索引 |
| **间隙锁** | 间隙 | 索引间隙 | 兼容（不锁记录） | `WHERE id > 10` 防止幻读 |
| **Next-Key Lock** | 行+间隙 | 记录+前间隙 | X 互斥 | RR 默认行锁升级 |
| **插入意向锁** | 间隙 | 插入前间隙 | 与间隙锁冲突 | `INSERT` 前置检查 |

**兼容性矩阵**（✓兼容 ✗冲突）：

| 请求\持有 | X (排他) | S (共享) | IX | IS | Gap | Next-Key | Insert Intention |
|-----------|----------|----------|-----|-----|-----|----------|------------------|
| **X**     | ✗        | ✗        | ✗   | ✗   | ✗   | ✗        | ✗                |
| **S**     | ✗        | ✓        | ✗   | ✓   | ✗   | ✗        | ✗                |
| **IX**    | ✗        | ✗        | ✓   | ✓   | ✓   | ✗        | ✗                |
| **IS**    | ✗        | ✓        | ✓   | ✓   | ✓   | ✓        | ✓                |
| **Gap**   | ✗        | ✗        | ✓   | ✓   | ✓   | ✗        | ✗                |
| **Next-Key**| ✗      | ✗        | ✗   | ✓   | ✗   | ✗        | ✗                |
| **Insert Int**| ✗    | ✗        | ✗   | ✓   | ✗   | ✗        | ✓                |

> **记忆口诀**：X 与所有冲突；S 只与 X 冲突；IX/IS 只与 X/S 冲突；Gap 只与 X/Next-Key/Insert Intention 冲突；Insert Intention 只与 Gap/Next-Key 冲突。

---

## 二、行锁实现原理：从索引到 Lock Struct

### 2.1 核心数据结构

```cpp
// InnoDB 锁结构 (简化自 lock0lock.h)
struct lock_t {
    trx_t*      trx;              // 所属事务
    dict_index_t* index;          // 所属索引
    lock_mode_t mode;             // LOCK_X | LOCK_S | LOCK_GAP | LOCK_ORDINARY | LOCK_INSERT_INTENTION
    lock_type_t type;             // LOCK_REC | LOCK_TABLE
    ulint       hash;             // Hash 值 (space_id + page_no + n_bits)
    bitmap_t    bitmap;           // 位图：每 bit 对应一条记录 (heap_no)
    // 链表指针
    lock_t*     trx_locks_next;   // 事务锁链表
    lock_t*     trx_locks_prev;
};

// 记录锁位图映射：heap_no -> bit
// heap_no: 页内记录偏移 (从 0 开始，0=infimum, 1=supremum, 2+=用户记录)
```

### 2.2 行锁加锁流程源码逻辑

```cpp
// row0sel.cc: row_search_for_mysql() -> 行锁加锁入口
// 核心函数：lock_rec_lock()

bool lock_rec_lock(
    lock_mode_t mode,        // 锁模式
    dict_index_t* index,     // 索引
    const ulint* rec,        // 记录指针
    lock_type_t type,        // LOCK_REC
    trx_t* trx,              // 事务
    ulint n_bits             // 位图长度
) {
    // 1. 计算 hash，定位 Lock Sys Hash 表桶
    ulint hash = lock_rec_hash(space_id, page_no, n_bits);
    
    // 2. 遍历桶内链表，查找是否已有同页锁对象
    for (lock_t* lock = HASH_GET(list, hash); lock; lock = lock->hash_next) {
        if (lock->index == index && lock->space == space_id && lock->page_no == page_no) {
            // 3. 找到现有锁对象，设置对应 heap_no 的 bit
            lock_rec_add_to_queue(lock, mode, heap_no);
            return true;  // 成功
        }
    }
    
    // 4. 未找到，创建新 lock_t，分配 bitmap，设置 bit
    lock_t* lock = lock_create(...);
    lock_rec_add_to_queue(lock, mode, heap_no);
    
    // 5. 插入 Hash 表 & 事务锁链表
    HASH_INSERT(list, hash, lock);
    trx_lock_add(lock, trx);
    
    return true;
}
```

### 2.3 关键点：heap_no 与位图

```text
数据页结构 (16KB)：
┌─────────────────────────────────────┐
│ Page Header (56 bytes)              │
├─────────────────────────────────────┤
│ Infimum Record (heap_no=0)          │  系统最小记录
├─────────────────────────────────────┤
│ User Record 1 (heap_no=2)           │  实际数据
├─────────────────────────────────────┤
│ User Record 2 (heap_no=3)           │
├─────────────────────────────────────┤
│ ...                                 │
├─────────────────────────────────────┤
│ Supremum Record (heap_no=1)         │  系统最大记录
├─────────────────────────────────────┤
│ Page Directory (稀疏索引)           │
├─────────────────────────────────────┤
│ Free Space                          │
└─────────────────────────────────────┘

Lock Bitmap (每 bit 对应一个 heap_no)：
bit 0: Infimum  (间隙锁左边界)
bit 1: Supremum (间隙锁右边界)
bit 2: Record 1
bit 3: Record 2
...
```

**Next-Key Lock 本质**：锁住 **记录本身 + 记录前的间隙**
- 对应 bitmap：`bit[heap_no]` (记录) + `bit[heap_no-1]` (前间隙)

---

## 三、MVCC 多版本并发控制：读不加锁的秘密

### 3.1 核心组件

```
MVCC 核心三件套：
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Undo Log       │  │  ReadView       │  │  Hidden Columns │
│  (版本链)       │  │  (可见性判断)   │  │  (DB_TRX_ID,    │
│                 │  │                 │  │   DB_ROLL_PTR)  │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

### 3.2 隐式列与版本链

```sql
-- 每行记录隐藏列 (不显示在 SHOW CREATE TABLE)
-- 可通过 SELECT * FROM information_schema.innodb_sys_tables 间接验证

CREATE TABLE t (
    id INT PRIMARY KEY,
    name VARCHAR(20)
);

-- 物理存储实际包含：
-- DB_ROW_ID      (6 bytes)  隐式主键 (无主键时)
-- DB_TRX_ID      (6 bytes)  最后修改事务 ID
-- DB_ROLL_PTR    (7 bytes)  指向 Undo Log 的指针
```

**版本链演示**：

```sql
-- 初始
INSERT INTO t VALUES (1, 'Alice');  -- trx_id=100

-- 更新 1
UPDATE t SET name='Bob' WHERE id=1;  -- trx_id=200

-- 更新 2
UPDATE t SET name='Charlie' WHERE id=1;  -- trx_id=300

-- 版本链 (最新在前)：
/*
Record (name='Charlie') 
  DB_TRX_ID=300, DB_ROLL_PTR=0x7f...  ───►
Undo Log (name='Bob')    
  DB_TRX_ID=200, DB_ROLL_PTR=0x7f...  ───►
Undo Log (name='Alice')  
  DB_TRX_ID=100, DB_ROLL_PTR=NULL
*/
```

### 3.3 ReadView 可见性判断算法

```cpp
// 源码: read0read.cc: read_view_sees_trx_id()
// ReadView 结构
struct ReadView {
    trx_id_t   low_limit_id;    // 活跃事务最小 ID (未提交最小)
    trx_id_t   up_limit_id;     // 已提交最大 ID + 1 (高水位)
    trx_id_t*  trx_ids;         // 活跃事务 ID 列表 (排序)
    ulint      n_trx_ids;
    trx_id_t   creator_trx_id;  // 创建者事务 ID
};

// 可见性判断核心逻辑
bool trx_id_is_visible(trx_id_t trx_id, ReadView* view) {
    // 1. 自己修改的，可见
    if (trx_id == view->creator_trx_id) return true;
    
    // 2. 比已提交最大还小，可见
    if (trx_id < view->up_limit_id) return true;
    
    // 3. 比活跃最小还大，不可见 (未提交)
    if (trx_id >= view->low_limit_id) return false;
    
    // 4. 在活跃列表中，不可见 (未提交)
    // 二分查找 trx_ids 数组
    if (binary_search(view->trx_ids, view->n_trx_ids, trx_id)) {
        return false;
    }
    
    // 5. 不在活跃列表，且 < low_limit_id，可见 (已提交)
    return true;
}
```

### 3.4 RC vs RR 隔离级别的 ReadView 生命周期

| 隔离级别 | ReadView 创建时机 | 可见性差异 |
|----------|-------------------|------------|
| **READ COMMITTED (RC)** | **每个 SELECT 语句开始** | 每次查询看到最新已提交数据，可能不可重复读 |
| **REPEATABLE READ (RR)** | **事务第一个 SELECT** | 事务内所有 SELECT 共享同一 ReadView，可重复读 |

```sql
-- RC 场景：不可重复读
-- 事务 A                    -- 事务 B
START TRANSACTION;           START TRANSACTION;
SELECT * FROM t WHERE id=1;  -- 'Alice'
                             UPDATE t SET name='Bob' WHERE id=1;
                             COMMIT;
SELECT * FROM t WHERE id=1;  -- 'Bob' (RC: 不同！RR: 仍是 'Alice')
COMMIT;
```

### 3.5 MVCC 与锁的配合：一致性非锁定读 vs 锁定读

```sql
-- 一致性非锁定读 (普通 SELECT) - MVCC，不加锁
SELECT * FROM t WHERE id = 1;

-- 锁定读 (显式加锁)
SELECT * FROM t WHERE id = 1 LOCK IN SHARE MODE;      -- S 锁 (RC/RR: 记录锁)
SELECT * FROM t WHERE id = 1 FOR UPDATE;              -- X 锁 (RC/RR: 记录锁)
SELECT * FROM t WHERE id = 1 FOR UPDATE SKIP LOCKED;  -- 跳过已锁行 (JDK 8+)
SELECT * FROM t WHERE id = 1 FOR UPDATE NOWAIT;       -- 立即失败不等待 (JDK 8+)
SELECT * FROM t WHERE id = 1 FOR SHARE SKIP LOCKED;   -- S 锁跳过
```

---

## 四、事务隔离级别与实现差异

### 4.1 四大隔离级别对比

| 隔离级别 | 脏读 | 不可重复读 | 幻读 | InnoDB 实现 |
|----------|------|------------|------|-------------|
| **READ UNCOMMITTED** | ✓ | ✓ | ✓ | 直接读最新版本，无 ReadView |
| **READ COMMITTED** | ✗ | ✓ | ✓ | 每次 SELECT 生成 ReadView |
| **REPEATABLE READ** | ✗ | ✗ | ✗* | 事务首次 SELECT 生成 ReadView + Next-Key Lock |
| **SERIALIZABLE** | ✗ | ✗ | ✗ | 读加 S 锁，写加 X 锁，串行化 |

> *InnoDB RR 通过 **Next-Key Lock** 解决幻读，但**不完全等同于串行化**（如范围查询+插入仍可能并发）。

### 4.2 幻读在 RR 下的解决：Next-Key Lock

```sql
-- 事务 A (RR)
START TRANSACTION;
SELECT * FROM t WHERE id > 10 FOR UPDATE;  -- 加 Next-Key Lock
-- 锁住：(10, +∞) 间隙 + 所有 id>10 的记录

-- 事务 B
INSERT INTO t VALUES (15, 'x');  -- 阻塞！等待间隙锁释放
```

**Next-Key Lock 加锁规则（RR 默认）**：
1. 唯一索引等值查询 → **Record Lock** (退化)
2. 唯一索引范围查询 → **Next-Key Lock**
3. 非唯一索引等值查询 → **Next-Key Lock**
4. 非唯一索引范围查询 → **Next-Key Lock**
5. 无索引全表扫描 → **表锁** (退化，性能灾难)

---

## 五、死锁：原理、复现、排查、预防

### 5.1 死锁必要条件（Coffman 条件）

1. **互斥**：资源独占
2. **持有并等待**：持有锁等待新锁
3. **不可抢占**：锁只能主动释放
4. **循环等待**：环形等待链

### 5.2 经典死锁复现：两事务反向更新

```sql
-- 表结构
CREATE TABLE account (
    id INT PRIMARY KEY,
    balance DECIMAL(10,2)
);
INSERT INTO account VALUES (1, 100), (2, 100);

-- 事务 A                        -- 事务 B
START TRANSACTION;               START TRANSACTION;
UPDATE account SET balance=balance-10 WHERE id=1;  -- 锁 id=1
                                    UPDATE account SET balance=balance-10 WHERE id=2;  -- 锁 id=2
UPDATE account SET balance=balance+10 WHERE id=2;  -- 等待 id=2 (B持有)
                                    UPDATE account SET balance=balance+10 WHERE id=1;  -- 等待 id=1 (A持有)
-- DEADLOCK!                      -- DEADLOCK!
```

**死锁日志解读** (`SHOW ENGINE INNODB STATUS`)：

```
------------------------
LATEST DETECTED DEADLOCK
------------------------
2026-09-09 14:30:15 0x7f8b1c00
*** (1) TRANSACTION:
TRANSACTION 12345, ACTIVE 0.001 sec
2 lock struct(s), heap size 1136, 1 row lock(s)
MySQL thread id 10, OS thread handle 14023, query id 100 localhost root
UPDATE account SET balance=balance+10 WHERE id=2
*** (1) WAITING FOR THIS LOCK TO BE GRANTED:
RECORD LOCKS space id 5 page no 3 n bits 72 index PRIMARY of `test`.`account`
trx id 12345 lock_mode X locks rec but not gap waiting
Record lock, heap no 3

*** (2) TRANSACTION:
TRANSACTION 12346, ACTIVE 0.001 sec
2 lock struct(s), heap size 1136, 1 row lock(s)
MySQL thread id 11, OS thread handle 14024, query id 101 localhost root
UPDATE account SET balance=balance+10 WHERE id=1
*** (2) WAITING FOR THIS LOCK TO BE GRANTED:
RECORD LOCKS space id 5 page no 3 n bits 72 index PRIMARY of `test`.`account`
trx id 12346 lock_mode X locks rec but not gap waiting
Record lock, heap no 2

*** WE ROLL BACK TRANSACTION (1)
```

### 5.3 死锁检测机制

```cpp
// InnoDB 死锁检测：Wait-for Graph (等待图)
// 源码: lock0dead.cc

// 1. 事务等待锁时，构建等待边：Trx_A --waits for--> Trx_B
// 2. 定期/每次等待时 DFS 检测环
// 3. 发现环 → 选择代价最小的事务回滚 (权重：修改行数、锁数、undo量)

// 关键配置：
// innodb_deadlock_detect = ON (默认开启)
// innodb_lock_wait_timeout = 50s (默认，超时回滚)
```

**Wait-for Graph 示例**：

```
Trx_A (持有 id=1 锁) ───等待 id=2───► Trx_B (持有 id=2 锁)
  ▲                                              │
  │                                              ▼
  └──────────等待 id=1──────────── Trx_B (持有 id=2 锁)
  
检测到环：Trx_A → Trx_B → Trx_A
```

### 5.4 线上死锁排查 SOP

```bash
# 1. 实时监控死锁
# 开启死锁记录到错误日志
innodb_print_all_deadlocks = ON

# 2. 定期收集
grep -i "deadlock" /var/log/mysql/error.log | tail -20

# 3. 实时查看当前锁等待
SELECT * FROM performance_schema.data_lock_waits;
SELECT * FROM sys.innodb_lock_waits;  -- 更友好视图

# 4. 关键信息提取
# - 死锁 SQL
# - 事务隔离级别
# - 索引情况 (EXPLAIN)
# - 并发量、频次
```

### 5.5 死锁预防最佳实践

| 策略 | 实施方式 | 适用场景 |
|------|----------|----------|
| **统一访问顺序** | 业务代码按主键升序更新多行 | 批量更新、转账 |
| **缩短事务** | 少用 `SELECT ... FOR UPDATE`，拆分大事务 | 高并发核心链路 |
| **合理索引** | 避免无索引全表锁、间隙锁过大 | 所有写操作 |
| **降低隔离级别** | RC + `binlog_format=ROW` | 允许不可重复读的业务 |
| **重试机制** | 捕获 `Lock wait timeout` / `Deadlock` 重试 | 幂等接口 |
| **SKIP LOCKED** | `SELECT ... FOR UPDATE SKIP LOCKED` | 队列、抢单场景 |

```java
// 代码级重试模板
@Retryable(retryFor = {CannotAcquireLockException.class, DeadlockLoserDataAccessException.class},
           maxAttempts = 3, backoff = @Backoff(delay = 10, multiplier = 2))
@Transactional
public void transfer(Long from, Long to, BigDecimal amount) {
    // 按 ID 升序加锁
    List<Account> accounts = accountMapper.selectByIdsOrderByIdAsc(List.of(from, to));
    // 业务逻辑...
}
```

---

## 六、InnoDB 核心参数调优指南

### 6.1 Buffer Pool (最关键)

```ini
# 内存 64GB 服务器建议
innodb_buffer_pool_size = 48G        # 物理内存 70-80%
innodb_buffer_pool_instances = 8     # 1GB/实例，减少竞争
innodb_buffer_pool_chunk_size = 1G   # 扩容单位
innodb_old_blocks_pct = 37           # Young/Old 区比例
innodb_old_blocks_time = 1000        # 1秒内不晋升 Young (防全表扫描污染)
```

### 6.2 Redo Log (写性能核心)

```ini
innodb_log_file_size = 2G            # 单文件大小 (总 4G)
innodb_log_files_in_group = 2
innodb_log_buffer_size = 64M         # 大事务增大
innodb_flush_log_at_trx_commit = 1   # 1=最安全，2=高性能(丢1秒), 0=最高性能(丢数据)
innodb_flush_method = O_DIRECT       # 绕过 OS Cache，防双缓存
```

### 6.3 Undo Log & Purge

```ini
innodb_undo_tablespaces = 2          # 独立 Undo 表空间 (便于回收)
innodb_max_undo_log_size = 2G        # 超过触发截断
innodb_purge_threads = 4             # Purge 并行线程
innodb_purge_rseg_truncate_frequency = 128
```

### 6.4 锁与并发

```ini
innodb_lock_wait_timeout = 10        # 降低等待时间，快速失败重试
innodb_deadlock_detect = ON          # 必须开启
innodb_spin_wait_delay = 6           # 自旋等待 (高并发可调大)
```

---

## 七、引擎选型：InnoDB vs MyISAM vs 其他

| 场景 | 推荐引擎 | 理由 |
|------|----------|------|
| **OLTP 核心业务** | **InnoDB** | 事务、行锁、MVCC、崩溃恢复 |
| **只读/日志/归档** | MyISAM / Archive | 表锁、全文索引、压缩存储 |
| **实时分析/OLAP** | ClickHouse / Doris / StarRocks | 列存、向量化、MPP |
| **高并发计数器** | InnoDB + `innodb_thread_concurrency` / Redis | 原子操作 |
| **临时表/中间结果** | MEMORY / TempTable (MySQL 8+) | 内存级速度 |
| **全文搜索** | InnoDB (8.0+ 支持) / Elasticsearch | 专业搜索引擎更强 |

---

## 八、线上典型事故案例复盘

### 案例 1：无索引 UPDATE 导致全表锁

```sql
-- 事故 SQL
UPDATE user SET status=1 WHERE phone='13800000000';  -- phone 无索引

-- 现象：全库阻塞，所有写入等待
-- 原因：无索引 → 全表扫描 → 每行加 Next-Key Lock → 退化表锁
-- 解决：加索引 `idx_phone(phone)`
```

### 案例 2：大事务导致 Undo Log 爆盘

```sql
-- 事故：一次性删除 500 万行
DELETE FROM logs WHERE create_time < '2025-01-01';

-- 后果：
-- 1. Undo Log 暴涨 50GB+
-- 2. Purge 线程跟不上，History List Length 飙升
-- 3. 后续 SELECT 因遍历版本链极慢
-- 4. 主从延迟严重

-- 解决：分批删除 (每次 5000 行 + 间隔 sleep)
```

### 案例 3：隐式转换导致索引失效

```sql
-- 表：id VARCHAR(20) PRIMARY KEY
-- 事故：
SELECT * FROM t WHERE id = 12345;  -- 字符串列比数字，隐式转换 CAST(id AS UNSIGNED)

-- 现象：全表扫描，锁全表
-- 解决：WHERE id = '12345' 或 WHERE CAST(id AS CHAR) = '12345'
```

### 案例 4：Gap Lock 导致插入死锁

```sql
-- 事务 A
SELECT * FROM t WHERE id > 100 FOR UPDATE;  -- 锁 (100, +∞)

-- 事务 B
INSERT INTO t VALUES (200, 'x');  -- 等待 Gap Lock

-- 事务 A 提交后，B 成功
-- 但高并发下大量插入排队，RT 飙升

-- 优化：改用 RC 隔离级别 (无 Gap Lock) 或精确索引定位
```

---

## 九、MySQL 8.0 新特性锁增强

| 特性 | 说明 | 价值 |
|------|------|------|
| **`NOWAIT` / `SKIP LOCKED`** | `FOR UPDATE NOWAIT` / `FOR UPDATE SKIP LOCKED` | 队列、抢单、分布式锁无阻塞 |
| **即时加列** | `ALTER TABLE ... ADD COLUMN ... INSTANT` | 元数据修改不锁表 |
| **降序索引** | `INDEX idx_desc (col DESC)` | 避免 filesort |
| **不可见索引** | `ALTER TABLE ... ALTER INDEX ... INVISIBLE` | 验证索引效果无风险 |
| **Histogram 统计** | `ANALYZE TABLE ... UPDATE HISTOGRAM` | 优化器更准 |
| **CTE / 窗口函数** | `WITH RECURSIVE` / `ROW_NUMBER()` | 复杂查询简化 |

---

## 十、12 年经验总结：MySQL 锁事务避坑清单

```
□ 所有表必须有显式主键 (推荐 BIGINT AUTO_INCREMENT)
□ 所有 WHERE/JOIN/ORDER BY 列建立索引 (EXPLAIN 验证)
□ 禁止 SELECT *，显式列出列名
□ 事务尽可能短，不在事务中调用 RPC/HTTP
□ 批量操作分批 (500-1000 行/批)
□ 统一多表更新顺序 (按表名/主键排序)
□ 显式指定隔离级别 (RC + ROW binlog 通用最优)
□ 监控：Innodb_row_lock_waits, Innodb_deadlocks, History_list_length
□ 开启 innodb_print_all_deadlocks = ON
□ 定期 PURGE 大事务表 (pt-archiver / 分区表)
□ 备库同步延迟监控 (Seconds_Behind_Master / GTID)
□ 备份验证：定期恢复演练
```

---

## 十一、核心源码阅读路线图

```
1. 事务系统
   trx0trx.cc: trx_t 结构、事务状态机、提交/回滚

2. 锁系统
   lock0lock.cc: lock_t 结构、加锁/释放、死锁检测
   lock0types.h: 锁模式位掩码定义

3. MVCC
   read0read.cc: ReadView 创建、可见性判断、purge
   undo0undo.cc: Undo Log 写入、版本链维护

4. 索引与行锁
   row0sel.cc: 行搜索、加锁入口
   btr0cur.cc: B+Tree 游标操作

5. Buffer Pool
   buf0buf.cc: 页管理、LRU、Flush
   buf0flu.cc: 刷盘策略

6. Redo Log
   log0log.cc: Redo 写入、Checkpoint、恢复
```

---

> **更新于 2026-09-09** | 持续补充中...
>
> **参考源码**：MySQL 8.0.39 / Percona Server 8.0 / MariaDB 10.11 InnoDB 源码
>
> **推荐工具**：
> - `sys.schema_table_lock_waits` / `sys.innodb_lock_waits`
> - `pt-deadlock-logger` 死锁收集
> - `mysqlslap` / `sysbench` 压测验证
> - `innodb_ruby` / `innodb_diagrams` 页结构可视化